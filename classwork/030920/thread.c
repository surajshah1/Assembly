#include <iostream>

struct {
	void* src;
	void* dst;
	unsigned int size;
} buf_info;

void * {
	
	return NULL;
}

int main(){
	
	// Thread id
	pthread_t threadID;
	p1;
	p2;

	//Create a thread that will function threadFunc()
	struct buf_info x, y;
	x.src = p1;
	x.dst = p2;
	x.size = 100*1024*1024/2;

        y.src = p1 + (100*1024*1024);
        y.dst = p2 + (100*1024*1024);
        y.size = 100*1024*1024/2;

	int err = pthread_create(&threadId, NULL, &threadFunc, &x);
	// Check if thread s created sucessfully

}
